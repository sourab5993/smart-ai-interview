# Git Workflow Rule

After completing and validating code changes (e.g. running lint and tests), always automatically stage, commit with a concise, descriptive message, and push to GitHub `origin main`.
